# fastapi 0.100 tirait starlette 0.27 (8 avis de securite sur la couche HTTP de
# CHAQUE requete). Monter fastapi impose pydantic v2 (fastapi>=0.139 exige >=2.9).
fastapi==0.139.0
uvicorn[standard]==0.34.0
# uvicorn >= 0.30 a SUPPRIME uvicorn.workers.UvicornWorker (la classe que gunicorn
# charge en prod). Elle vit desormais dans ce paquet. Sans lui, le conteneur meurt
# au demarrage avec 'class uri invalid or not found' — et la CI ne le voyait pas,
# car elle ne lancait jamais la vraie commande de production.
uvicorn-worker==0.4.0
gunicorn==23.0.0
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.11.1
pydantic==2.13.4
# python-jose dropped: CVE-2024-33663 (algorithm confusion) and unmaintained.
PyJWT==2.13.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
# 0.0.6 carried CVE-2024-24762 (ReDoS on a malformed multipart boundary) — and
# this app accepts invoice uploads from the internet.
python-multipart==0.0.32
celery==5.3.1
redis==4.6.0
# boto3 1.26 pinned urllib3<1.27, which carries 4 advisories. Bumping it is
# what unlocks urllib3 2.x.
boto3==1.43.46
urllib3>=2.7.0
python-dotenv==1.2.2
pgvector==0.3.6

# AI assistant (Claude / Anthropic)
anthropic>=0.69.0

# Video import: transcript (YouTube captions / audio download) + STT (OpenAI Whisper)
youtube-transcript-api>=0.6.2
yt-dlp>=2024.8.6
openai>=1.40.0

# OCR providers & observability
# httpx >=0.25 required by anthropic 0.69+; 0.27 keeps the OCR httpx usage working.
httpx==0.27.2
prometheus-client==0.20.0
# Optional: install only when using the Google Document AI provider
# google-cloud-documentai==2.29.2

# testing
pytest==9.1.1
pytest-asyncio==0.22.0

