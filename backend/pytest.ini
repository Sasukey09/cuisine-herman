[pytest]
addopts = --cov=app --cov-report=term-missing
python_files = tests/*.py
# pytest 8 no longer prepends the rootdir to sys.path on its own, so `import app`
# stopped resolving. Declaring it explicitly is the supported way (and works on 7 too).
pythonpath = .
