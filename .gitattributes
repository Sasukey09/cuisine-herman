# Normalize all text files to LF in the repo (required for shell scripts in Linux containers).
* text=auto eol=lf

# Binaries (never normalize)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.exe binary
